import React from 'react';

const ImportAsync = () => (
    <div>
        <h2>Asynchronous Import</h2>
        <div>Change me once the project is running! I will update cleanly with 4.9.2 but fail and the page fully reloads with 4.10.2</div>
    </div>
);

export default ImportAsync;